En la carpeta modelo se encuentra el notebook con los modelos utilizados (Perceptrón multicapa y Random Forest)

En la carpeta optimizador se encuentra el notebook MAIN para ejecutar el problema y los diferentes módulos .py de los que se compone el optimizador. 
Dentro de esta carpeta, hay una llamada pruebas que se ha utilizado para probar los diferentes algoritmoe y hallar las métricas. 

Para calcular el hipervolúmen tienes que normalizar previamente los valores y establecer los límites entre 0 y 1 porque si no da 0 siempre.
